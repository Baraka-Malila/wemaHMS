from django.urls import path
from . import views

app_name = 'reception'

urlpatterns = [
    # Reception endpoints will be added here
    # path('dashboard/', views.ReceptionDashboardView.as_view(), name='dashboard'),
    # path('appointments/', views.AppointmentListView.as_view(), name='appointments'),
    # path('patients/', views.PatientListView.as_view(), name='patients'),
]
